// Copy to LicenseSecret.swift and paste in the secret from the login keychain:
//     security find-generic-password -s com.samuelmittman.macid.licensing -a hmac-secret -w
enum LicenseSecret {
    static let raw = "PASTE-THE-BASE64-SECRET-HERE"
}
